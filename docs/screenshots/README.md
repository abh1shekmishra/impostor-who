# Screenshots

Drop the following PNGs here (portrait phone frames look best) and they will
render in the top of the main [README](../../README.md):

| File | Screen to capture |
|------|-------------------|
| `home.png`   | Home / landing screen |
| `reveal.png` | Role reveal (the "you are the impostor" / secret-word card) |
| `vote.png`   | Voting screen |
| `result.png` | Result / scoreboard |

Optional: add a `demo.gif` of one full round (Home → Reveal → Clue → Vote →
Result) and embed it near the top of the README for maximum impact.

Tip: capture at a phone viewport (e.g. Chrome DevTools device toolbar, iPhone
14 Pro, 3x scale) for crisp, consistent frames.
